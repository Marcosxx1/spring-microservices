server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/LOANS_MS
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false